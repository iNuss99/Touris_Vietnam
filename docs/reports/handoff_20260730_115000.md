# Handoff Report: Tối Ưu Trang Cài Đặt, Chế Độ Giả Lập Góc Nhìn Role Cho Super Admin & Tự Động Gửi Mật Khẩu Qua Email

**Date:** 30/07/2026  
**Project:** Touris_Vietnam  
**Location:** `e:\Touris_Vietnam\docs\reports\handoff_20260730_115000.md`  

---

## 1. Mục tiêu Session

Session này tập trung vào 4 nhóm yêu cầu quan trọng của người dùng:
1. **Tối ưu hóa Trang Cài đặt (SettingsView)**: Tái cấu trúc giao diện gọn gàng, loại bỏ các phần không cần thiết (Quản lý nhân sự, Thẻ thông báo tĩnh) và tích hợp các mục cá nhân vào 1 khung thẻ thống nhất với bố cục hàng ngang 2 cột.
2. **Tính năng Giả lập Góc nhìn Role (Role Impersonation / View As)**: Cho phép Super Admin kiểm tra trải nghiệm giao diện của các vai trò (*Super Admin, Kinh doanh, Biên tập viên, Người xem*) trên TopHeader mà không cần đăng xuất.
3. **Cơ chế Cấp lại mật khẩu & Gửi Email tự động cho Nhân sự**: Đảm bảo mật khẩu tạm thời khi khởi tạo/reset tài khoản được tự động gửi qua Gmail SMTP, đồng thời hiển thị trên màn hình với nút **Sao chép 1-click** cho Admin.
4. **Sửa các lỗi giao diện & Runtime Exception**: Khắc phục triệt để lỗi `roleLabels is not defined`, lặp tiêu đề trang Quản lý nhân sự và lỗi định danh role hiển thị sai.

---

## 2. Các thay đổi kỹ thuật (Technical Changes)

### 2.1. Frontend (React / Vite / Tailwind CSS)

- **Trang Cài đặt ([SettingsView.jsx](file:///e:/Touris_Vietnam/frontend/src/components/Dashboard/SettingsView.jsx))**:
  - Loại bỏ hoàn toàn component `<UserManagement />` và thẻ "Thông báo" tĩnh không dùng.
  - Hợp nhất **Thông tin cá nhân** và **Đổi mật khẩu** vào 1 thẻ container duy nhất.
  - Bố cục: Thẻ chứa Header thông tin tài khoản (Avatar, Tên, Email, Nút Đăng xuất) và Grid 2 cột (`grid grid-cols-1 lg:grid-cols-2 gap-8`) trình bày song song form Thông tin cá nhân & Đổi mật khẩu.

- **Tính năng Giả lập Góc nhìn Role ([AuthContext.jsx](file:///e:/Touris_Vietnam/frontend/src/context/AuthContext.jsx), [TopHeader.jsx](file:///e:/Touris_Vietnam/frontend/src/components/Dashboard/TopHeader.jsx) & [index.jsx](file:///e:/Touris_Vietnam/frontend/src/components/Dashboard/index.jsx))**:
  - `AuthContext.jsx`: Thêm state `viewAsRole`, hàm `setViewAsRole`, cờ `isSuperAdmin` và lưu trữ `sessionStorage` (`touris_view_as_role`).
  - `TopHeader.jsx`: Bổ sung nút bấm Biểu tượng mắt 👁️ dạng Dropdown menu dành riêng cho Super Admin với các tùy chọn: *Super Admin, Kinh doanh, Biên tập viên, Người xem*.
  - `Dashboard/index.jsx`: Thêm thanh Cảnh báo màu cam ở đầu trang khi ở chế độ xem role (`⚠️ Đang trải nghiệm giao diện với Góc nhìn: [Role]... [Thoát chế độ xem]`) và tự động chuyển tab nếu role đang giả lập không có quyền vào tab hiện tại.

- **Quản lý Nhân sự & Mật khẩu ([UserManagement.jsx](file:///e:/Touris_Vietnam/frontend/src/components/Dashboard/UserManagement.jsx))**:
  - Đổi tiêu đề thẻ trong từ "Quản lý nhân sự" thành **"Danh sách tài khoản"** để tránh trùng lặp tiêu đề ngoài.
  - Thêm Modal hiển thị **Mật khẩu tạm thời** có nút **"Sao chép" (1-click Copy)** và biểu tượng trạng thái gửi mail tự động (Xanh: Đã gửi tự động; Cam: Chưa gửi được mail do lỗi SMTP).

- **Môi trường Frontend ([frontend/.env](file:///e:/Touris_Vietnam/frontend/.env))**:
  - Tạo file cấu hình trỏ `VITE_BACKEND_URL=http://localhost:5000` phục vụ test live gửi mail qua SMTP local backend.

### 2.2. Backend & Dịch vụ Email (Node.js / Express / Nodemailer)

- **Cấu hình Gmail SMTP ([email_service.js](file:///e:/Touris_Vietnam/backend/src/services/email_service.js))**:
  - Chuyển transporter sang cấu hình `service: 'gmail'` kết nối qua cổng SSL (465) đảm bảo độ tin cậy và không bị tường lửa block.
  - Trả về đối tượng `{ success, error }` chi tiết cho cả 2 hàm `sendWelcomeEmail` và `sendPasswordResetEmail`.

- **Cập nhật User Controller ([users.controller.js](file:///e:/Touris_Vietnam/backend/src/controllers/users.controller.js))**:
  - Bổ sung trả về `tempPassword`, `emailSent` và `emailError` trong cả 2 API `createUser` (POST `/api/users`) và `resetUserPassword` (POST `/api/users/:id/reset-password`).

---

## 3. Kết quả xác minh (Verification Results)

- **Giả lập Góc nhìn Role**:
  - Đăng nhập Super Admin ➔ Đổi sang góc nhìn *Kinh doanh* ➔ Sidebar lập tức ẩn các tab Quản trị & Báo cáo CEO. Thanh Cảnh báo màu cam hiển thị ở trên cùng. Bấm "Thoát chế độ xem" ➔ Quay lại giao diện Super Admin chuẩn.
- **Trang Cài đặt gọn gàng**:
  - Giao diện 1 thẻ duy nhất với layout 2 cột hàng ngang hiển thị đẹp mắt, thông tin cá nhân và đổi mật khẩu cân đối.
- **Gửi Email Mật khẩu Nhân sự**:
  - Test lệnh gửi Gmail SMTP tới `duy.hoang030199@gmail.com` trả về `250 2.0.0 OK (Gửi thành công)`.
  - Trên UI Admin, sau khi bấm Cấp lại mật khẩu ➔ Hiển thị Modal mật khẩu tạm thời kèm nút Sao chép và thông báo đã gửi mail thành công.

---

## 4. Next Steps & Khuyến nghị tiếp theo

1. **Đồng bộ Environment Variables lên Vercel Dashboard**: Thêm `GMAIL_USER` và `GMAIL_APP_PASSWORD` vào Vercel Settings ➔ Environment Variables để ứng dụng sản xuất trên Vercel tự động gửi mail.
2. **Nhật ký Hoạt động (Audit Logs)**: Ghi lại log các hành động chuyển đổi góc nhìn role của Super Admin để phục vụ kiểm toán bảo mật.
3. **Cơ chế Đổi Mật khẩu Lần đầu (Must Change Password)**: Bắt buộc tài khoản nhân sự đổi mật khẩu cá nhân mới ngay ở lần đầu tiên đăng nhập bằng mật khẩu tạm.
